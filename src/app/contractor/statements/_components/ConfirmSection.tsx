// RETIRED (Phase 2, 2026-08-17) — contractor statement confirm/review panel.
//
// Showed the review deadline and a "Confirm" button asking the contractor to
// approve their statement before payment. That gate no longer exists: pay flows
// straight from approved work to a remittance.
//
// Kept as an inert stub rather than deleted so the retirement is visible in the
// tree and nothing dangles. Renders nothing — a historical statement must not
// look like an outstanding task.

export function ConfirmSection() {
  return null
}
