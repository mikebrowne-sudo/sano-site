// RETIRED (Phase 1, 2026-08-17) — legacy new-contractor-pay-run form.
//
// Submitted to a stubbed action whose inserts are blocked in the database. The
// parent route redirects, making this unreachable.
//
// Kept as an inert stub rather than deleted so the retirement is visible in the
// tree and nothing dangles. Renders nothing.

export function NewPayRunForm() {
  return null
}
