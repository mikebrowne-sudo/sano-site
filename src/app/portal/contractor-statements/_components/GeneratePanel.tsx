// RETIRED (Phase 2, 2026-08-17) — "Generate / refresh drafts" panel.
//
// Created draft statements for a period and linked approved payables to them.
// Statement generation is retired; the backing action is stubbed.
//
// Kept as an inert stub rather than deleted so the retirement is visible in the
// tree and nothing dangles. Renders nothing.

export function GeneratePanel() {
  return null
}
